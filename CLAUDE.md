# Claude Instructions

Zoho Task ID: 2543412000001583003

- Scope all work to `BI1-T110` and this repository.
- Read `README.md`, `TASK.md`, and `STATUS.md` before changing files.
- Follow existing architecture and coding conventions; keep changes small and explicit.
- Run the repository's relevant tests, validators, and syntax checks before reporting completion.
- Document meaningful decisions and update `STATUS.md` with progress, blockers, and next actions.
- Never expose secrets, tokens, `.env` contents, or private customer data.
- Do not push directly to the default branch without explicit approval.
- Do not modify Zoho runtime systems unless explicitly approved.
- Keep implementation in `scripts/`, documentation in `docs/`, and approved supporting files in `artifacts/`.
